import { defineConfig } from 'wxt';
import pkg from './package.json';

export default defineConfig({
  outDir: 'dist',
  manifest: {
    name: 'CreatorWeave',
    description: 'Provides web_search and web_fetch capabilities for in-browser Agents',
    version: pkg.version,
    permissions: ['scripting', 'tabs', 'storage', 'alarms', 'declarativeNetRequest', 'declarativeNetRequestFeedback', 'offscreen', 'notifications'],
    host_permissions: ['<all_urls>'],
    // icons and action icon are auto-generated by WXT from public/icon.png
  },
});
