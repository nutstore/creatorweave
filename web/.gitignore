node_modules
dist
out
.next
coverage
test-results

# Regenerated by dev/build scripts — never track
# (prepare-static-assets.mjs / pack-skills.sh / prepare-pwa.mjs)
public/extension
public/chrome-extension.zip
public/skills
public/sw.js
# prepare-static-assets copies node_modules/pyodide here as a REAL directory
# (dereference: true — the old symlink pointed at an absolute path that broke
# on other machines). Always regenerated by the same script, never tracked.
public/assets/pyodide
