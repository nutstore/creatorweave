{
  "name": "creatorweave",
  "version": "0.1.0",
  "private": true,
  "packageManager": "pnpm@10.21.0",
  "workspaces": [
    "packages/*",
    "web",
    "mobile-web",
    "relay-server"
  ],
  "resolutions": {
    "esbuild": "^0.21.5"
  },
  "scripts": {
    "test": "cd web && pnpm test",
    "check": "make lint && make typecheck && make test"
  }
}
